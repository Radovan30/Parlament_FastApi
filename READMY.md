# FastAPI Restaurant Backend

1. **Naklonujte repozitář** a přejděte do složky `fastApiParlament`.
2. Vytvořte virtuální prostředí:
   ```bash
   python -m venv venv
   source venv/bin/activate  # macOS/Linux
   # nebo .\venv\Scripts\activate na Windows